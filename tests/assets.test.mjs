import test from 'node:test';import assert from 'node:assert/strict';import {readFile,readdir} from 'node:fs/promises';
for(const name of ['warden','hollow','grave-hound','revenant','bellkeeper','elder','healer','smith','watchman'])test(`${name} is a complete generated GLB with finite mesh data`,async()=>{const data=await readFile(new URL(`../dist/assets/models/${name}.glb`,import.meta.url));assert.equal(data.readUInt32LE(),0x46546c67);assert.equal(data.readUInt32LE(4),2);assert.equal(data.readUInt32LE(8),data.length);const jsonLen=data.readUInt32LE(12),doc=JSON.parse(data.toString('utf8',20,20+jsonLen));assert.ok(doc.meshes.length>20);assert.ok(doc.nodes.some(n=>n.name==='body'));assert.ok(doc.nodes.some(n=>n.name.startsWith('leg')));const binStart=28+jsonLen;for(const a of doc.accessors){const view=doc.bufferViews[a.bufferView];assert.ok(view.byteOffset+view.byteLength<=doc.buffers[0].byteLength);if(a.componentType===5126){for(let b=0;b<view.byteLength;b+=4)assert.ok(Number.isFinite(data.readFloatLE(binStart+view.byteOffset+b)));}}});
